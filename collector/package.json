{
  "name": "xing-collector",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy && wrangler deploy -c wrangler.alerts.toml",
    "deploy:waits": "wrangler deploy",
    "deploy:alerts": "wrangler deploy -c wrangler.alerts.toml",
    "dev": "wrangler dev --test-scheduled",
    "dev:alerts": "wrangler dev -c wrangler.alerts.toml --test-scheduled"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.3"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}
